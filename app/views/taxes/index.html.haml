- @no12 = true

.container.container_12
  .grid_12
    %h2 The Taxes
    #wrapper
      - @taxes.each do |t|
        %div{:style => 'width: 450px; padding-bottom: 2em;'}
          = render :partial => 'shared/tax', :object => t
    :javascript
      $(document).ready(function() { 
        $('#wrapper').masonry({ columnWidth: 470, singleMode: true,  });
      });
  
