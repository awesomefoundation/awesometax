.container_12
  .grid_8
    %h2 Start a Chapter
    
    %br/
    
    - form_for @tax do |f|
      = f.label "Name Your Chapter"
      .input
        = f.text_field :name, :style => 'width: 100%;'
        .help-block Most likely your city's name. 5-40 characters.
      %br/
      = f.label "Summary"
      .input
        = f.text_area :description, :style => 'height: 15em; width: 100%;'
        .help-block
          Tell the world about your chapter, what it's accomplished, and its hopes and dreams.
          Charm us, woo us. Write at least 20 characters, but as much as you'd like.
          Feel free to use
          = link_to 'Markdown', 'http://daringfireball.net/projects/markdown/basics', :target => '_blank'
          formatting.
      %br/
      = f.label "Monthly Minimum"
      .input
        $
        = f.text_field :goal, :class => 'span2'
        .help-block
          The "tipping point" for this chapter. The monthly funding and payout will only happen if
          your funders have pledged at least this much. Otherwise, nobody pays and nobody receives, and you'll
          have to wait until next month's cycle.
          %em Leave it blank for no minimum.
      %br/
  
      %h3 PayPal Details
      - unless Rails.env == 'production'
        %p
          %b TESTING NOTE: Don't enter real PayPal info here, just use the info we've pre-filled for you. :)
          
      = f.label "Email Address"
      .input
        = f.text_field :paypal_email, :style => 'width: 100%;'
        .help-block
          The email address you'll receive funding at. We'll verify that this is registered, so the
          first and last names you provide below must match what's on the PayPal account.
      %br/
      = f.label "First Name"
      .input
        = text_field_tag :paypal_first, @paypal_first, :style => 'width: 100%;'
      %br/
      = f.label "Last Name"
      .input
        = text_field_tag :paypal_last, @paypal_last, :style => 'width: 100%;'
      %br/
      %br/ 
      = f.submit 'Create It'
      
    .grid_4
