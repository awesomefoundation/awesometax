%h2 Widget Testing

- if Tax.count == 0
  %p There aren't any taxes yet so we can't show you a widget. Oops.
  
- else
  #lovetax_widget{'data-id' => Tax.first.id, 'data-host' => "http://#{request.host}"}
  = javascript_include_tag("http://#{request.host}/javascripts/xss_magic.js")